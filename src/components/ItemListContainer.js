import React from "react";

const ItemListContainer  = (props) =>{
    return(
        <div id="catalogo">
            <h1>{props.mensaje}</h1>
        </div>
    );
}

export default ItemListContainer